@model IEnumerable<EcommerceWebApplication.Models.EF.Customer>

@{

    ViewBag.Title = "Index";
}
<div class="row">
    <div class="col-md-6">
        <h2>List of Users:</h2>
        <div class="dropdown">
            <a href="#" class="btn dropdown-toggle" data-toggle="dropdown" style="padding: 5px 5px 5px 5px">List of Users<b class="caret"></b></a>
            <ul class="dropdown-menu">
                @foreach (var item in Model)
                {
                    <li style="padding: 12px 16px">@item.CustomerName</li>
                }
            </ul>
        </div>
    </div>
    <div class="col-md-6">

    </div>
</div>


@*@Html.ActionLink(", "#", "#", new { customerId = item.CustomerID }, null)*@

